Descripción:  Trabajando con Arrays y métodos:

Crear un array con 30 posiciones que contiene números aleatorios entre 0 y 20.

Imprimir el array creado anteriormente.

Crear un array con la siguiente información: Batman, Superman, Krusty, Bob, Mel y Barney.

Eliminar el último elemento dentro del array (use métodos de array ).

Imprimir la posición del array donde se encontró 'Superman' (use métodos de array para buscar el contenido).

Agregue los siguientes elementos al final del array: Carl, Lenny, Burns y Lisa (use métodos de array ).

Ordenar los elementos del array e imprimir el array ordenado (use métodos de array ).

Eliminar desde la posición número 4, 2 elementos, incluyendo el cuarto elemento. (use métodos de array ).

Añadir estos elementos al array (en la posición de inicio): Manzana, Melón, Sandía (use métodos de array ).

Crear una copia del array con el nombre micopia y que contenga los elementos de tercero a quinto (use métodos de array ).

Añadir el elemento "Monitor" al final del array (use métodos de array ).

Crear un nuevo array que contiene el primer array y "micopia" concatenados. Nombre del nuevo array como Tercero (use métodos de array ).


///////////////////////////////////////////////////////////////////////////////////////////////////

Description: Working with Arrays and methods:

Create an array with 30 positions containing random numbers between 0 and 20.

Print the array created earlier.

Create an array with the following information: Batman, Superman, Krusty, Bob, Mel and Barney.

Remove the last element in the array (use methods array).

Print the position of the array where 'Superman' was found (use methods to search the content array).

Add the following items to the end of the array: Carl, Lenny, Burns and Lisa (use methods array).

Sort the array elements and print the orderly array (use methods array).

Remove from the No. 4 position, 2 elements, including the fourth element. (Use methods array).

Add these items to the array (in the home position): Apple, Melon, Watermelon (use methods array).

Create a copy of the array with the micopia name and containing the elements of third-fifth (use methods array).

Add the "Monitor" at the end of the array (use methods array) element.

Create a new array containing the first array and "micopia" concatenated. Name of the new array as Third (use methods array).